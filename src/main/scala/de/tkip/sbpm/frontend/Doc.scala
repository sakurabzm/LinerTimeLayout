//CALCULATESPACES(g) 计算画布大小

//LPSTREE(topology, type, parent, length) Langest path spanning tree 计算最长的分支

//COMPUTEBRANCHDIMENSIONS(n, parent)  计算宽度和深度

//PRELIMINARYLAYOUT(f)   初步的布局

//PREPROCESS(g)  对于不只有一个起点的图，需要添加一个起点和终点

//DRAW()

//DRAWFRAGMENT(f, x, y)

//DRAWNODE(n, x y)

//DRAWFREENODES(y)

//DRAWEDGES()